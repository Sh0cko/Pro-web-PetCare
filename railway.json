{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd PetCare && python manage.py migrate && gunicorn PetCare.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "on_failure"
  }
}
